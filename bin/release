#!/bin/bash
PACKS="ruby nodejs clojure python java gradle scala php go"

for PACK in $PACKS; do
  bin="/tmp/heroku-buildpack-${PACK}/bin"

  $bin/detect  /tmp/app &>/dev/null || continue
  $bin/release /tmp/app && exit 0
done

